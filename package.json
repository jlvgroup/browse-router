{
  "name": "browse-router",
  "version": "0.0.1",
  "private": true,
  "description": "Finicky-style URL router for Android. BrowseRouter gives you per-URL browser selection on mobile, with first-class support for foldables (VIVO X Fold 5, Samsung Flip 7).",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm -r --filter './packages/*' build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r exec rm -rf node_modules dist .turbo"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
